--1. How many orders were placed in January? 9681

SELECT COUNT(orderID) as 'Total Orders' FROM BIT_DB.JanSales WHERE length(orderID) = 6 AND orderid <> 'Order ID'; 

--2. How many of those orders were for an iPhone? 379

SELECT COUNT(orderID) FROM BIT_DB.JanSales WHERE length(orderID) = 6 AND orderid <> 'Order ID' AND Product = 'iPhone'; 

--3. Select the customer account numbers for all the orders that were placed in February.

SELECT * FROM BIT_DB.FebSales JOIN BIT_DB.customers ON BIT_DB.FebSales.orderID = BIT_DB.customers.order_id; 

--4. Which product was the cheapest one sold in January, and what was the price? 2.99

SELECT Min(price) FROM BIT_DB.JanSales;

--5. What is the total revenue for each product sold in January? (Revenue can be calculated using the number of products sold and the price of the products).

SELECT Product, price FROM BIT_DB.JanSales;  

--6. Which products were sold in February at 548 Lincoln St, Seattle, WA 98101, how many of each were sold, and what was the total revenue?
--7. How many customers ordered more than 2 products at a time in February, and what was the average amount spent for those customers?
